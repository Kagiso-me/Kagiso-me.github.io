---
import Layout from '../layouts/Layout.astro';
---

<Layout title="Digest" description="Ops log — infrastructure changes, deployments, and incidents.">

  <div class="page-wrap">

    <!-- ── Header ── -->
    <div class="page-header fade-up">
      <div>
        <p class="mono" style="color:var(--color-peach);margin-bottom:0.25rem">~/digest</p>
        <h1 style="margin:0">Ops Digest</h1>
      </div>
      <p class="hero-sub">
        A running log of infrastructure changes — deployments, fixes, incidents, and architectural decisions.
        Sourced directly from the <a href="https://github.com/Kagiso-me/homelab-infrastructure" target="_blank" rel="noopener" class="link-inline">homelab-infrastructure</a> changelog.
      </p>
    </div>

    <!-- ── Filter bar ── -->
    <div class="filter-bar fade-up" style="animation-delay:0.05s">
      <button class="filter-btn active" data-type="all">All</button>
      <button class="filter-btn" data-type="DEPLOY">Deploy</button>
      <button class="filter-btn" data-type="CONFIG">Config</button>
      <button class="filter-btn" data-type="OPS">Ops</button>
      <button class="filter-btn" data-type="FIX">Fix</button>
      <button class="filter-btn" data-type="SECURITY">Security</button>
      <button class="filter-btn" data-type="NETWORK">Network</button>
      <button class="filter-btn" data-type="INCIDENT">Incident</button>
      <button class="filter-btn" data-type="DECISION">Decision</button>
      <div class="filter-spacer"></div>
      <span class="mono" id="entry-count" style="color:var(--color-subtle);font-size:0.8rem"></span>
    </div>

    <!-- ── Timeline ── -->
    <div id="digest-entries" class="fade-up timeline" style="animation-delay:0.1s">
      <!-- skeleton -->
      <div class="skeleton" style="height:80px;border-radius:12px;margin-bottom:0.75rem"></div>
      <div class="skeleton" style="height:80px;border-radius:12px;margin-bottom:0.75rem"></div>
      <div class="skeleton" style="height:80px;border-radius:12px;margin-bottom:0.75rem"></div>
      <div class="skeleton" style="height:80px;border-radius:12px;margin-bottom:0.75rem"></div>
      <div class="skeleton" style="height:80px;border-radius:12px;margin-bottom:0.75rem"></div>
    </div>

  </div>

</Layout>

<style is:global>
  .page-wrap {
    max-width: 900px;
    margin: 0 auto;
    padding: 3rem 1.5rem 5rem;
  }

  .page-header {
    margin-bottom: 2.5rem;
  }

  h1 { font-size: clamp(1.75rem, 4vw, 2.5rem); margin-bottom: 0.75rem; }

  .hero-sub {
    color: var(--color-subtle);
    font-size: 0.95rem;
    line-height: 1.7;
    max-width: 620px;
    margin: 0.75rem 0 0;
  }

  .link-inline {
    color: var(--color-peach);
    text-decoration: none;
  }
  .link-inline:hover { text-decoration: underline; }

  /* ── Filter bar ── */
  .filter-bar {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    flex-wrap: wrap;
    margin-bottom: 2rem;
    padding-bottom: 1.5rem;
    border-bottom: 1px solid var(--color-muted);
  }

  .filter-spacer { flex: 1; }

  .filter-btn {
    font-family: var(--font-mono);
    font-size: 0.72rem;
    padding: 0.3rem 0.75rem;
    border: 1px solid var(--color-muted);
    border-radius: 6px;
    color: var(--color-subtle);
    background: transparent;
    cursor: pointer;
    transition: all 0.15s;
    text-transform: uppercase;
    letter-spacing: 0.05em;
  }

  .filter-btn:hover  { border-color: var(--color-peach); color: var(--color-peach); }
  .filter-btn.active { border-color: var(--color-peach); color: var(--color-peach); background: color-mix(in srgb, var(--color-peach) 10%, transparent); }

  /* ── Timeline container ── */
  .timeline {
    position: relative;
    padding-left: 2rem;
  }

  /* The vertical line — drawn via a pseudo on the container */
  .timeline::before {
    content: '';
    position: absolute;
    left: 0.45rem;
    top: 0.5rem;
    bottom: 0;
    width: 2px;
    background: linear-gradient(
      to bottom,
      var(--color-peach),
      var(--color-mauve) 40%,
      var(--color-muted) 80%,
      transparent
    );
    border-radius: 1px;
  }

  /* ── Month divider ── */
  .month-divider {
    position: relative;
    font-family: var(--font-mono);
    font-size: 0.72rem;
    text-transform: uppercase;
    letter-spacing: 0.12em;
    color: var(--color-subtle);
    margin: 2.25rem 0 1rem -2rem;
    padding: 0.2rem 0.75rem 0.2rem 1.75rem;
    background: var(--color-base);
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
  }

  /* Diamond on the timeline for each month */
  .month-divider::before {
    content: '◆';
    position: absolute;
    left: -1.6rem;
    color: var(--color-peach);
    font-size: 0.6rem;
    background: var(--color-base);
    padding: 2px 0;
  }

  .month-divider:first-child { margin-top: 0; }

  /* ── Entry card ── */
  .entry-card {
    display: block;
    text-decoration: none;
    background: var(--color-surface);
    border: 1px solid var(--color-muted);
    border-radius: 12px;
    padding: 1.1rem 1.4rem;
    margin-bottom: 0.65rem;
    transition: border-color 0.2s, transform 0.2s, box-shadow 0.2s;
    cursor: pointer;
    position: relative;
  }

  /* Timeline dot */
  .entry-card::before {
    content: '';
    position: absolute;
    left: -1.8rem;
    top: 50%;
    transform: translateY(-50%);
    width: 9px;
    height: 9px;
    border-radius: 50%;
    background: var(--color-muted);
    border: 2px solid var(--color-base);
    transition: background 0.2s, box-shadow 0.2s;
  }

  .entry-card:hover {
    border-color: var(--color-peach);
    transform: translateX(3px);
    box-shadow: 0 4px 20px rgba(0,0,0,0.3);
  }

  .entry-card:hover::before {
    background: var(--color-peach);
    box-shadow: 0 0 8px var(--color-peach);
  }

  /* Colour-coded dots by type */
  .entry-card[data-type="DEPLOY"]::before   { background: var(--color-ok); }
  .entry-card[data-type="CONFIG"]::before   { background: var(--color-sapphire); }
  .entry-card[data-type="OPS"]::before      { background: var(--color-mauve); }
  .entry-card[data-type="FIX"]::before      { background: var(--color-peach); }
  .entry-card[data-type="SECURITY"]::before { background: var(--color-warn); }
  .entry-card[data-type="NETWORK"]::before  { background: var(--color-gold); }
  .entry-card[data-type="INCIDENT"]::before { background: var(--color-crit); }
  .entry-card[data-type="DECISION"]::before { background: #a6e3a1; }

  .entry-card.no-detail { cursor: default; }
  .entry-card.no-detail:hover { transform: none; border-color: var(--color-muted); box-shadow: none; }
  .entry-card.no-detail:hover::before { box-shadow: none; }

  .entry-row {
    display: flex;
    align-items: flex-start;
    gap: 1rem;
    flex-wrap: wrap;
  }

  .entry-meta {
    display: flex;
    align-items: center;
    gap: 0.6rem;
    flex-shrink: 0;
    padding-top: 0.15rem;
  }

  .entry-date {
    font-family: var(--font-mono);
    font-size: 0.72rem;
    color: var(--color-subtle);
    white-space: nowrap;
  }

  .type-badge {
    font-family: var(--font-mono);
    font-size: 0.65rem;
    font-weight: 600;
    padding: 0.2rem 0.5rem;
    border-radius: 4px;
    text-transform: uppercase;
    letter-spacing: 0.06em;
    white-space: nowrap;
  }

  .type-DEPLOY   { background: color-mix(in srgb, var(--color-ok)       12%, transparent); color: var(--color-ok); }
  .type-CONFIG   { background: color-mix(in srgb, var(--color-sapphire) 12%, transparent); color: var(--color-sapphire); }
  .type-OPS      { background: color-mix(in srgb, var(--color-mauve)    12%, transparent); color: var(--color-mauve); }
  .type-FIX      { background: color-mix(in srgb, var(--color-peach)    12%, transparent); color: var(--color-peach); }
  .type-SECURITY { background: color-mix(in srgb, var(--color-warn)     12%, transparent); color: var(--color-warn); }
  .type-NETWORK  { background: color-mix(in srgb, var(--color-gold)     12%, transparent); color: var(--color-gold); }
  .type-INCIDENT { background: color-mix(in srgb, var(--color-crit)     12%, transparent); color: var(--color-crit); }
  .type-DECISION { background: color-mix(in srgb, #a6e3a1               12%, transparent); color: #a6e3a1; }
  .type-default  { background: color-mix(in srgb, var(--color-subtle)   12%, transparent); color: var(--color-subtle); }

  .entry-body { flex: 1; min-width: 0; }

  .entry-title {
    font-size: 0.95rem;
    font-weight: 600;
    color: var(--color-heading);
    line-height: 1.4;
    margin-bottom: 0.25rem;
  }

  .entry-footer {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    margin-top: 0.5rem;
  }

  .entry-commit {
    font-family: var(--font-mono);
    font-size: 0.7rem;
    color: var(--color-subtle);
  }

  .detail-link {
    font-family: var(--font-mono);
    font-size: 0.7rem;
    color: var(--color-peach);
    display: flex;
    align-items: center;
    gap: 0.25rem;
  }

  .skeleton {
    background: linear-gradient(90deg, var(--color-surface) 25%, var(--color-overlay) 50%, var(--color-surface) 75%);
    background-size: 200% 100%;
    animation: shimmer 1.5s infinite;
  }

  @keyframes shimmer {
    0%   { background-position: 200% 0; }
    100% { background-position: -200% 0; }
  }

  @media (max-width: 640px) {
    .entry-row { flex-direction: column; gap: 0.5rem; }
    .filter-bar { gap: 0.4rem; }
  }
</style>

<script>
  let allEntries: any[] = [];
  let activeType = 'all';

  const TYPE_COLORS: Record<string, string> = {
    DEPLOY: 'type-DEPLOY', CONFIG: 'type-CONFIG', OPS: 'type-OPS',
    FIX: 'type-FIX', SECURITY: 'type-SECURITY', NETWORK: 'type-NETWORK',
    INCIDENT: 'type-INCIDENT', DECISION: 'type-DECISION',
  };

  function formatDate(d: string) {
    // d is either "YYYY-MM-DD" or "YYYY-MM"
    if (d.length === 7) {
      const [y, m] = d.split('-');
      return new Date(+y, +m - 1).toLocaleDateString('en-ZA', { month: 'short', year: 'numeric' });
    }
    return new Date(d + 'T12:00:00').toLocaleDateString('en-ZA', { day: 'numeric', month: 'short', year: 'numeric' });
  }

  function monthKey(d: string) {
    return d.substring(0, 7); // "YYYY-MM"
  }

  function monthLabel(ym: string) {
    const [y, m] = ym.split('-');
    return new Date(+y, +m - 1).toLocaleDateString('en-ZA', { month: 'long', year: 'numeric' });
  }

  function render(entries: any[]) {
    const container = document.getElementById('digest-entries');
    if (!container) return;

    const countEl = document.getElementById('entry-count');
    if (countEl) countEl.textContent = `${entries.length} entries`;

    if (!entries.length) {
      container.innerHTML = `<div class="mono" style="color:var(--color-subtle);padding:2rem 0;text-align:center">No entries for this filter.</div>`;
      return;
    }

    // Group by month
    const byMonth: Record<string, any[]> = {};
    for (const e of entries) {
      const mk = monthKey(e.date);
      if (!byMonth[mk]) byMonth[mk] = [];
      byMonth[mk].push(e);
    }

    let html = '';
    for (const mk of Object.keys(byMonth).sort().reverse()) {
      html += `<div class="month-divider">${monthLabel(mk)}</div>`;
      for (const e of byMonth[mk]) {
        const typeClass = TYPE_COLORS[e.type] ?? 'type-default';
        const href = e.hasDetail ? `/digest/entry?slug=${e.slug}` : null;
        const tag = href ? 'a' : 'div';
        const hrefAttr = href ? `href="${href}"` : '';
        const noDetailClass = href ? '' : ' no-detail';

        html += `
        <${tag} ${hrefAttr} class="entry-card${noDetailClass}" data-type="${e.type}">
          <div class="entry-row">
            <div class="entry-meta">
              <span class="entry-date">${formatDate(e.date)}</span>
              <span class="type-badge ${typeClass}">${e.type}</span>
            </div>
            <div class="entry-body">
              <div class="entry-title">${e.title}</div>
              <div class="entry-footer">
                ${e.commit ? `<span class="entry-commit">${e.commit}</span>` : ''}
                ${e.hasDetail ? `<span class="detail-link">read more →</span>` : ''}
              </div>
            </div>
          </div>
        </${tag}>`;
      }
    }
    container.innerHTML = html;
  }

  async function loadDigest() {
    try {
      const res = await fetch('/data/digest.json');
      if (!res.ok) throw new Error('no data');
      allEntries = await res.json();
      applyFilter();
    } catch {
      const container = document.getElementById('digest-entries');
      if (container) container.innerHTML = `<div class="mono" style="color:var(--color-subtle);padding:2rem 0">Failed to load digest data.</div>`;
    }
  }

  function applyFilter() {
    const filtered = activeType === 'all'
      ? allEntries
      : allEntries.filter(e => e.type === activeType);
    render(filtered);
  }

  // Filter button clicks
  document.querySelectorAll('.filter-btn').forEach(btn => {
    btn.addEventListener('click', () => {
      document.querySelectorAll('.filter-btn').forEach(b => b.classList.remove('active'));
      btn.classList.add('active');
      activeType = (btn as HTMLElement).dataset.type ?? 'all';
      applyFilter();
    });
  });

  loadDigest();
</script>
